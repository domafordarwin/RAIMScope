# RAIMScope 점검 보고서 검토·검증 결과

- 검토 대상: `ANALYSIS_REPORT_2026-07-26.md`
- 검토 일자: 2026-07-26
- 검토 범위: 보고서의 주장, 수치, 노트북 실행 기록, 데이터셋 구성, 학습 결과, 저장소 정리 상태 및 결론의 적절성

## 1. 종합 판정

검토 대상 보고서의 핵심 결론은 대체로 타당하다.

> `Onion-Cell-Merged-v5-2` 데이터셋을 사용한 학습 자체는 정상적으로 완료됐지만, 이 데이터셋의 `d/nd` 이진 클래스는 프로젝트가 채택한 4개 세포 상태 클래스와 직접 호환되지 않는다.

실제 노트북 로그에서 100 epoch 학습 완료와 높은 내부 검증 지표가 확인되며, `data.yaml`의 상대경로도 현재 설치된 Ultralytics에서 정상적으로 해석된다. 따라서 해당 실험을 단순한 “학습 실패”로 기록한 것은 부정확하다는 보고서의 판단은 적절하다.

다만 다음 문제가 있어 현재 문서는 **조건부 적합**으로 판정한다.

1. Git에 포함된 평문 API 키를 발견하지 못한 중대한 보안 점검 누락
2. mAP50 및 클래스 인스턴스 수의 수치 오류
3. 클래스 불균형, 과적합 및 실패 메모의 출처에 대한 과도한 인과·의도 단정
4. “병합 불가능”이라는 표현의 과도한 일반화
5. 객체 탐지와 분류, 세포주기 상태와 표준 체세포분열 단계의 용어 혼용
6. `README.md`, `DATASETS.md` 등 관련 문서가 검토 결과와 모순된 상태

이 항목들을 수정하면 보고서의 신뢰성과 활용 가능성이 충분히 확보될 것으로 판단한다.

---

## 2. 검증 방법

다음 원자료를 보고서 내용과 대조했다.

- `ANALYSIS_REPORT_2026-07-26.md`
- `Yolo11_Exec_01.ipynb` Cell 44~68의 코드와 저장된 실행 출력
- `Onion-Cell-Merged-v5-2/data.yaml`
- `phase-of-cell-6/data.yaml`
- `cell_OB_yolo11/data.yaml`
- `_archive/runs_root/detect/mitosis_yolo11n_detect-2/args.yaml`
- `_archive/runs_root/detect/mitosis_yolo11n_detect-2/results.csv`
- `_archive/runs_root/detect/mitosis_yolo11n_640-6/args.yaml`
- `_archive/runs_root/detect/mitosis_yolo11n_640-6/results.csv`
- 데이터셋 전체 YOLO 라벨 파일
- 현재 Git 이력, 원격 저장소 설정, 디렉터리와 아카이브 상태
- `README.md`, `DATASETS.md`, `.gitignore`, `train_improved.py`

또한 프로젝트 가상환경에서 `ultralytics.data.utils.check_det_dataset()`을 실행해 데이터 경로 해석 결과를 독립적으로 확인했다.

---

## 3. 보고서 주요 주장별 검증

### 3.1 `data.yaml` 상대경로가 학습 실패 원인이 아니라는 주장

**판정: 타당**

`Onion-Cell-Merged-v5-2/data.yaml`에는 다음과 같이 기록되어 있다.

```yaml
train: ../train/images
val: ../valid/images
test: ../test/images
```

현재 프로젝트 가상환경에서 `check_det_dataset()`을 호출한 결과, 다음 실제 경로로 정상 해석됐다.

```text
Onion-Cell-Merged-v5-2/train/images
Onion-Cell-Merged-v5-2/valid/images
Onion-Cell-Merged-v5-2/test/images
```

노트북 Cell 52도 수정되지 않은 동일 YAML을 사용해 학습을 정상 완료했다. 따라서 이 상대경로는 해당 실행에서 학습 실패 원인이 아니었다.

다만 이 동작은 Ultralytics의 경로 보정 로직에 의존할 수 있으므로, 이식성과 명확성을 위해 YAML을 `train/images`, `valid/images`, `test/images`로 정규화하는 것은 여전히 권장된다.

### 3.2 YAML 수정본 저장 위치 오타

**판정: 타당**

노트북 Cell 45에서 `yaml_onion`을 만들었지만 Cell 46에서는 다음 경로로 저장했다.

```python
with open("Mitosis-15/data.yaml", "w") as f:
    yaml.dump(yaml_onion, f)
```

의도한 `Onion-Cell-Merged-v5-2/data.yaml`이 아니며, 현재 실제 폴더명은 `Mitosis-1`이다. 따라서 저장 대상 오타라는 보고서의 판단은 맞다.

이 수정이 원래 데이터 YAML에 반영되지 않았음에도 Cell 52 학습이 정상 완료됐으므로, 해당 오타가 Onion 데이터 학습 결과에 직접 영향을 주지는 않았다.

### 3.3 `Onion-Cell-Merged-v5-2` 학습 성공 여부

**판정: 학습 실행 성공은 확인되나 수치 표기 수정 필요**

노트북과 보존된 실행 결과에서 다음 사항이 확인됐다.

- YOLO11n 사용
- 100 epoch 완료
- 조기 중단이나 크래시 없음
- best checkpoint 검증 완료
- validation 이미지 129장, 객체 3,902개

best checkpoint의 검증 결과는 다음과 같다.

| 항목 | 값 |
|---|---:|
| Precision | 0.874 |
| Recall | 0.831 |
| mAP50 | 0.899 |
| mAP50-95 | 0.714 |
| `d` mAP50 | 0.828 |
| `nd` mAP50 | 0.970 |

마지막 100 epoch의 mAP50은 약 `0.895`이고, 학습 종료 후 best checkpoint를 다시 검증한 mAP50은 약 `0.899`다.

원 보고서는 본문 표에는 `0.899`를 인용하면서 소제목과 결론에는 `0.895`를 사용했다. 다음과 같이 구분해 적어야 한다.

> 100번째 epoch의 mAP50은 0.895였으며, best checkpoint의 최종 검증 mAP50은 0.899였다.

이 결과는 “학습 프로세스가 기술적으로 정상 완료됐다”는 근거로 충분하다. 그러나 같은 데이터셋의 내부 validation 결과만으로 다른 현미경 환경에 대한 일반화 성능이나 실제 서비스 적합성까지 입증되지는 않는다. 별도 test 평가 또는 외부 데이터 검증이 필요하다.

### 3.4 클래스 스키마 불일치

**판정: 핵심 판단은 타당하나 표현 완화 필요**

데이터셋 스키마는 다음과 같다.

| 데이터셋 | 클래스 |
|---|---|
| `Onion-Cell-Merged-v5-2` | `d`, `nd` |
| `phase-of-cell-6` | `anaphasae`, `interphase`, `metaphase`, `telophase` |

`d`는 여러 분열 상태를 하나의 클래스에 포함할 수 있으므로 이를 특정 세포 상태 하나에 직접 대응시킬 수 없다. `nd` 역시 프로젝트의 `interphase`와 완전히 같은 의미인지 라벨 정의 검증이 필요하다.

따라서 두 데이터셋의 라벨을 변환 없이 합쳐 동일한 4-class 객체 탐지 모델을 학습하는 것은 부적절하다.

다만 “phase 분류 목표와 병합이 불가능하다”는 표현은 지나치게 절대적이다. 다음 표현이 더 정확하다.

> 현재 라벨을 그대로 유지한 직접 병합은 불가능하며, 활용하려면 재라벨링, 검증된 클래스 매핑, 계층형 모델 또는 멀티태스크 학습이 필요하다.

### 3.5 클래스 불균형 수치와 성능 영향

**판정: 불균형은 확인되지만 보고서 수치는 오류**

현재 데이터셋의 전체 YOLO 라벨을 다시 집계한 결과는 다음과 같다.

| 분할 | `d` | `nd` | 합계 |
|---|---:|---:|---:|
| train | 2,103 | 12,499 | 14,602 |
| valid | 533 | 3,369 | 3,902 |
| test | 277 | 1,737 | 2,014 |
| 전체 | **2,913** | **17,605** | **20,518** |

전체 비율은 약 `14.2 : 85.8`이다. 원 보고서의 `d 1,806 / nd 12,345`는 현재 데이터셋과 일치하지 않으므로 출처를 재확인하거나 위 수치로 수정해야 한다.

또한 `d`의 mAP50이 더 낮다는 사실만으로 그 원인을 클래스 불균형으로 확정할 수 없다. 객체 크기, 형태 다양성, 시각적 난이도, 라벨 품질과 같은 다른 요인도 영향을 줄 수 있다.

권장 문구는 다음과 같다.

> `d` 클래스의 인스턴스가 상대적으로 적고 mAP50도 낮게 나타났다. 클래스 불균형이 성능 차이에 영향을 주었을 가능성이 있으나, 인과관계를 확정하려면 추가 실험이 필요하다.

### 3.6 Cell 56의 “학습이 되지 않았다”는 메모 해석

**판정: 유력한 해석이지만 작성 의도를 확정할 수 없음**

노트북 실행 순서는 다음과 같다.

1. Cell 52: `Onion-Cell-Merged-v5-2` 학습
2. Cell 54: `mitosis_yolo11n_detect-2` checkpoint 로드
3. Cell 55: `cell_OB_yolo11/valid/images` 추론
4. Cell 56: “어떤 이유에인지 전혀 학습이 되지 않아 일단은 후퇴”

Cell 56이 Cell 55 직후에 있으므로, 메모가 `mitosis_yolo11n_detect-2` 결과를 가리킨다는 해석은 충분히 개연성이 있다.

보존된 `args.yaml`을 통해 `mitosis_yolo11n_detect-2`가 실제로 `cell_OB_yolo11/data.yaml`을 사용해 학습됐음도 확인됐다. 이 모델의 마지막 validation mAP50은 약 `0.207`에 불과하며, 일부 검증 이미지에서는 실제 라벨에 Interphase가 없는데도 70개 이상을 Interphase로 검출했다. 따라서 이 모델의 탐지 성능이 좋지 않았다는 판단은 근거가 있다.

하지만 다음 표현은 증거보다 강하다.

- 메모의 “실제 출처”라고 확정
- 모델 문제를 “과적합”으로 확정
- 모든 다수 검출을 “중복 검출”로 확정

낮은 validation 성능과 과다한 false positive는 확인되지만, 과적합인지 미수렴·라벨 품질·해상도·하이퍼파라미터 문제인지는 별도 진단이 필요하다. 작성자가 Cell 56에서 정확히 어느 실험을 지칭했는지도 셀 순서만으로 완전히 증명할 수 없다.

권장 문구는 다음과 같다.

> 셀 배치와 실행 순서를 고려할 때 Cell 56의 메모는 `mitosis_yolo11n_detect-2`의 추론 결과를 지칭했을 가능성이 가장 높다. 다만 작성자의 명시적 설명이 없어 이를 확정적인 사실로 단정할 수는 없다.

---

## 4. 프로젝트 구조 점검 내용 검증

### 4.1 확인된 내용

다음 조치 내역은 현재 저장소 상태와 일치한다.

- Git 저장소가 초기화되고 기존 원격 이력과 병합됨
- `origin`이 `domafordarwin/RAIMScope`를 가리킴
- 병합 커밋이 서로 다른 두 이력을 부모로 보존함
- `rock-paper-scissors` 프로젝트가 상위 별도 폴더로 분리됨
- 중복 ZIP 파일이 `_archive/duplicate_zips/`에 보존됨
- 구버전 실험이 `_archive/runs_root/`에 보존됨
- 현재 `runs/detect/`에는 `phase_cell_improved_v1_stage1/2`만 남아 있음
- `models/phase_cell_v1_best.pt`가 Stage 2 best checkpoint와 동일함
- `README.md`, `DATASETS.md`, `.gitignore`가 생성·정비됨

### 4.2 용량과 실험 수 표기

현재 확인되는 크기는 다음과 같다.

- `_archive/runs_root/`: 약 470 MiB
- 현재 `runs/`: 약 87 MiB
- `_archive/duplicate_zips/`: 약 21 MiB
- `onion_yolo_project/.venv`: 약 1.2 GiB(십진 단위로 약 1.3 GB)

아카이브에는 detect 실험 36개와 segment 실험 7개가 있다. 원 보고서의 “`runs/detect/`에 40개 가까이, 약 572MB”는 정리 전 측정값일 수 있으나 현재 상태만으로 정확히 재현되지는 않는다. 측정 시점과 포함 범위를 명시하는 것이 좋다.

### 4.3 가상환경 보류 판단

가상환경을 무리하게 이동하지 않은 판단은 운영 안정성 측면에서 합리적이다. 다만 장기적으로는 재현 가능한 `requirements.txt` 또는 잠금 파일을 검증하고, 새 가상환경에서 학습 스크립트가 정상 실행되는지 확인한 뒤 레거시 폴더와 실행환경의 결합을 해소하는 것이 바람직하다.

---

## 5. 보고서에서 누락된 중요 문제

### 5.1 평문 API 키 노출

**심각도: 매우 높음**

Git이 추적하는 `Yolo11_Exec_01.ipynb` Cell 59에 Roboflow API 키가 평문으로 포함되어 있다. 해당 키는 프로젝트 초기 정리 커밋에도 포함되어 원격 Git 이력에 반영된 상태다.

필요한 조치는 다음과 같다.

1. 노출된 키 즉시 폐기 및 재발급
2. 노트북의 평문 키를 환경 변수 참조 방식으로 변경
3. `.env`가 Git에서 제외되는지 재확인
4. 원격 저장소 공개 범위 확인
5. 필요한 경우 Git 이력에서 시크릿 제거
6. GitHub 등의 secret scanning 결과 확인

단순히 현재 파일에서 문자열만 삭제해서는 과거 Git 이력에 남은 키가 제거되지 않는다. 키 폐기가 가장 우선이다.

### 5.2 클래스명 오탈자

`phase-of-cell-6/data.yaml`과 관련 결과에는 `anaphasae`라는 클래스명이 사용된다. 이는 `anaphase`의 오탈자로 보인다.

클래스 인덱스를 바꾸지 않고 표시 이름만 일관되게 정정할 수 있는지 검토해야 한다. 이미 생성된 가중치의 클래스명 메타데이터, 학습 결과 문서, GUI 표시 이름도 함께 확인해야 한다.

### 5.3 과학적·기술적 용어

프로젝트의 네 클래스는 `interphase / metaphase / anaphase / telophase`이며, 표준적인 체세포분열 네 단계와 완전히 같지 않다. Interphase는 일반적으로 유사분열 자체의 단계로 보지 않으며, 프로젝트 스키마에는 Prophase가 없다.

또한 현재 모델은 YOLO 객체 탐지 모델이므로 기술적으로는 단순 “phase 분류”보다 “세포 위치 검출과 상태 클래스 예측을 수행하는 4-class 객체 탐지”가 정확하다.

보고서에서는 과학적 표준 분류와 프로젝트가 선택한 데이터셋 클래스 체계를 구분해 설명해야 한다.

### 5.4 문서 간 상태 불일치

검토 보고서의 결론과 달리 현재 문서에는 여전히 다음과 같이 기록되어 있다.

- `README.md`: `Onion-Cell-Merged-v5-2`를 “학습 실패, 원인 미확인”으로 표기
- `DATASETS.md`: 해당 데이터셋을 “실험 실패”로 표기

후속 조치 대상에는 기존 메모리뿐 아니라 `README.md`와 `DATASETS.md`도 반드시 포함해야 한다.

---

## 6. 수정 권고안

원 보고서를 최종 확정하기 전에 다음 순서로 수정하는 것을 권고한다.

### 필수

1. 노출된 Roboflow API 키 폐기 및 재발급
2. 보고서에 보안 점검 누락과 대응 조치 추가
3. mAP50을 “마지막 epoch 0.895 / best checkpoint 0.899”로 구분
4. 클래스 인스턴스 수를 재집계 결과로 정정
5. “불균형에 기인한다”를 가능성 표현으로 완화
6. Cell 56 메모의 대상을 확정 사실이 아닌 유력한 해석으로 기술
7. `detect-2` 문제를 “과적합” 대신 “낮은 검증 성능과 과다 false positive”로 기술
8. “병합 불가능”을 “현재 라벨 그대로 직접 병합 불가능”으로 수정

### 권장

1. `README.md`와 `DATASETS.md`의 실패 기록 갱신
2. `anaphasae` 오탈자 정정 계획 수립
3. “분류”를 “4-class 객체 탐지”로 정밀화
4. 프로젝트 클래스 체계가 표준 유사분열 단계와 다른 점 명시
5. Onion 데이터의 test split 또는 외부 데이터로 독립 평가
6. `d`와 `nd` 각각의 정의와 원천 데이터 라벨 정책 확인
7. 아카이브 용량과 실험 수에 측정 시점·포함 범위 명시

---

## 7. 권장 결론 문안

다음과 같이 결론을 수정하면 현재 근거 수준에 가장 잘 부합한다.

> `Onion-Cell-Merged-v5-2`를 사용한 YOLO11n 학습은 100 epoch를 정상 완료했으며, best checkpoint의 내부 validation mAP50은 0.899였다. 따라서 해당 실험은 기술적인 학습 실패로 볼 수 없다.
>
> 다만 이 데이터셋의 클래스는 `d/nd` 이진 스키마이고, 현재 프로젝트의 목표는 `interphase/metaphase/anaphase/telophase` 4-class 객체 탐지이므로 라벨을 그대로 유지한 직접 병합은 불가능하다. 활용하려면 원본 객체를 목표 클래스에 맞게 재라벨링하거나, 클래스 정의를 검증한 계층형·멀티태스크 파이프라인을 설계해야 한다.
>
> 노트북 Cell 56의 “학습이 되지 않았다”는 메모는 셀 순서상 `cell_OB_yolo11`로 학습한 `mitosis_yolo11n_detect-2`의 낮은 검증 성능과 과다 false positive를 지칭했을 가능성이 높다. 그러나 작성자의 명시적 설명이 없으므로 메모의 대상을 확정적으로 단정할 수는 없다.
>
> 별도로, Git에 포함된 노트북에서 평문 Roboflow API 키가 확인됐으므로 키 폐기·재발급과 시크릿 제거가 최우선 후속 조치다.

---

## 8. 최종 의견

원 보고서는 학습 로그와 데이터 스키마를 연결해 기존의 “원인 미확인 학습 실패” 기록을 재해석했다는 점에서 가치가 있다. 특히 상대경로 문제와 실제 학습 성공 여부를 실행 기록으로 구분한 접근은 적절하다.

그러나 정확한 감사·검증 보고서로 사용하려면 수치의 일관성, 인과관계 표현, 과학적 용어 및 보안 검토를 보강해야 한다. 본 검토에서 제시한 필수 수정사항을 반영한 뒤 최종본으로 확정하는 것이 적절하다.
